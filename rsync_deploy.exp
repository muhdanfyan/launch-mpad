#!/usr/bin/expect -f
set timeout 300
set password "Sipanda123#"
set host "157.10.252.74"
set user "sipanda"

spawn rsync -avz --exclude ".git" --exclude ".gemini" ./ $user@$host:/home/sipanda/launch-mpad/
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
